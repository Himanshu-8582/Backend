import { asyncHandler } from "../utils/asyncHandler";

const healthCheck=asyncHandler(async (req, res) => {
    // TODO: build a healthcheck res that simply returns the ok status as join with a message
});